@model IEnumerable<Tenant>

<h2>Tenant Index - Tenant sayısı (@Model.Count())</h2>

@if(Model.Count() > 0)
{
    foreach(var tenant in Model)
    {
        <tr>
            <td>@tenant.Name</td>
            <td>@tenant.CreatedDate</td>
        </tr>
    }
}