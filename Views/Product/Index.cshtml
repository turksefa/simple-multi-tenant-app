@model IEnumerable<Product>

<h2>Product Index - Ürün sayısı (@Model.Count())</h2>

@if(Model.Count() > 0)
{
    foreach(var product in Model)
    {
        <tr>
            <td>@product.Name</td>
            <td>@product.Price</td>
        </tr>
    }
}